for (let i = 0; i < 9; i++) {
    getElementById(i).registerListeners
}
                function registerListeners(id) {
                  document
                    .getElementById(id)
                    .addEventListener("change", updateSubmit);
                }

                function updateSumTotal() {
for (let i = 0; i < 9; i++) {
    getElementById(i).registerListeners
}

                  display.innerHTML = duration * rate + travelFee + " €";
                }